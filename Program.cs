  using System;
using System.Reflection.Metadata;

namespace MultiThreadingandTPL
{
    class Program
    {
        static void Main(string[] args)
        {
            //ForegroundAndBackgroundThreads fabthreads = new ForegroundAndBackgroundThreads();
            //ParameterizedTStart tStart = new ParameterizedTStart();
            //ThreadCallbacks callbacks = new ThreadCallbacks();
            //ThreadJoin threadJoin = new ThreadJoin();
            //DeadLocksInMT deadLocks = new DeadLocksInMT();
            DeadlocksInMTSolution deadlocksSolution = new DeadlocksInMTSolution();

        }
    }

}
